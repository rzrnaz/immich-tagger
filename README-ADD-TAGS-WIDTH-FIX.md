# PhotoAI Add Tags Width Fix

GUI-only layout fix.

The `Add Tags` checkbox was still clipping to `Add` on Windows display scaling. This widens the checkbox and gives the second options row more height.
