# PhotoAI Dry Run Label Update

This is a GUI-label-only update.

The checkbox label changes from:

```text
Dry run / preview only
```

to:

```text
Dry Run
```

Behavior is unchanged: when checked, the app previews what would happen but does not call Ollama and does not write files.
